# Base URL of the deployed FastAPI backend (Render). Used by src/services/apiClient.ts.
# Local development falls back to http://localhost:8000 if this is unset.
VITE_API_BASE_URL=http://localhost:8000
